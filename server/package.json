{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "db:reset": "set -a && . ./.env && set +a && psql -v ON_ERROR_STOP=1 \"$DATABASE_URL\" -f db/schema.sql && psql -v ON_ERROR_STOP=1 \"$DATABASE_URL\" -f db/seed.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.23.0"
  }
}
